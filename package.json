{
	"name": "tims_test",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"directories": {
		"doc": "doc"
	},
	"scripts": {
		"start": "npm-run-all --parallel start:server",
		"install:dependencies": "cd ./backend && npm install && cd ../api && npm install && cd ../frontend && npm install",
		"start:server": "node api"
	},
	"author": "",
	"license": "ISC"
}
